/**
 * @file Define a Page class, which extends Node and describes a type of file that is most 
 * likely going to become a full HTML page upon build.
 */

import Node from "./node";

class Page extends Node {

}

export default Page;